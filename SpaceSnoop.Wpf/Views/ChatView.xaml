<UserControl x:Class="SpaceSnoop.Wpf.Views.ChatView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:SpaceSnoop.Wpf.ViewModels.Chat"
             xmlns:chat="clr-namespace:SpaceSnoop.Wpf.Views.Chat"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance vm:ChatViewModel}"
             d:DesignHeight="700"
             d:DesignWidth="900">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <chat:ChatHistoryPanel Grid.Column="0"
                               Visibility="{Binding History.IsHistoryVisible, Converter={StaticResource BoolToVisibility}}" />

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <chat:ChatStateBanner Grid.Row="0" />
            <chat:ChatTranscript Grid.Row="1" />
            <chat:ChatComposer Grid.Row="2" />
        </Grid>
    </Grid>
</UserControl>
