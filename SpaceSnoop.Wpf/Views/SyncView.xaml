<UserControl x:Class="SpaceSnoop.Wpf.Views.SyncView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:SpaceSnoop.Wpf.ViewModels.Sync"
             xmlns:sync="clr-namespace:SpaceSnoop.Wpf.Views.Sync"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance vm:SyncViewModel}"
             d:DesignHeight="680"
             d:DesignWidth="1040">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <sync:SyncToolbar Grid.Row="0" />
        <sync:SyncLedgerStrip Grid.Row="1" />
        <sync:SyncProgressStrip Grid.Row="2" />
        <sync:SyncDiffPanel Grid.Row="3" />
    </Grid>
</UserControl>
