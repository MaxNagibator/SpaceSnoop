<UserControl x:Class="SpaceSnoop.Wpf.Views.DockerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:SpaceSnoop.Wpf.ViewModels.Docker"
             xmlns:docker="clr-namespace:SpaceSnoop.Wpf.Views.Docker"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance vm:DockerViewModel}"
             d:DesignHeight="760"
             d:DesignWidth="900">

    <ScrollViewer VerticalScrollBarVisibility="Auto"
                  HorizontalScrollBarVisibility="Disabled"
                  Padding="0,0,12,0">
        <StackPanel RequestBringIntoView="SuppressAutoScroll">
            <docker:DockerUnavailableView />
            <docker:DockerBucketsView />
            <docker:DockerGroupsView />
            <docker:DockerCleanupView x:Name="Cleanup" />
        </StackPanel>
    </ScrollViewer>
</UserControl>
