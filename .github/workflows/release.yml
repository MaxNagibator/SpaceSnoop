name: Release

on:
  release:
    types: [ published ]
  workflow_dispatch:
    inputs:
      tag:
        description: 'Select tag of release'
        required: true
        type: string

permissions:
  contents: write

jobs:
  release:
    runs-on: windows-2022
    defaults:
      run:
        working-directory: ./SpaceSnoop

    steps:
      - uses: actions/checkout@v4

      - name: Setup dotnet
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'
          cache: true
          cache-dependency-path: "**/packages.lock.json"

      - name: Determine release tag
        id: determine-tag
        run: |
          if ("${{ github.event_name }}" -eq "workflow_dispatch") {
            $tag = "${{ github.event.inputs.tag }}"
          } else {
            $tag = "${{ github.ref_name }}"
          }
          echo "TAG=$tag" >> $env:GITHUB_ENV

      - name: Publish
        run: |
          dotnet publish --configuration Release --runtime win-x64 --output .\publish\framework-dependent -p:PublishSingleFile=true -p:PublishReadyToRun=true --no-self-contained
          Rename-Item -Path .\publish\framework-dependent\SpaceSnoop.exe -NewName "SpaceSnoop_$env:TAG.exe"
          Compress-Archive -Path .\publish\framework-dependent\SpaceSnoop_$env:TAG.exe -DestinationPath SpaceSnoop_$env:TAG.zip

      - name: Publish portable
        run: |
          dotnet publish --configuration Release --runtime win-x64 --output .\publish\portable -p:PublishSingleFile=true -p:PublishReadyToRun=true -p:IncludeNativeLibrariesForSelfExtract=true --self-contained
          Rename-Item -Path .\publish\portable\SpaceSnoop.exe -NewName "SpaceSnoop_${env:TAG}_portable.exe"
          Compress-Archive -Path .\publish\portable\SpaceSnoop_${env:TAG}_portable.exe -DestinationPath SpaceSnoop_${env:TAG}_portable.zip

      - name: Upload
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release upload $env:TAG `
            .\publish\framework-dependent\SpaceSnoop_$env:TAG.exe `
            SpaceSnoop_$env:TAG.zip `
            .\publish\portable\SpaceSnoop_${env:TAG}_portable.exe `
            SpaceSnoop_${env:TAG}_portable.zip
